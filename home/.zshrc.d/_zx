#compdef zx

_zx() {
  local groups
  groups=()
  for group in $(find "$HOME/.zx" -mindepth 1 -maxdepth 1 -type d); do
    groups+=("${group##*/}")
  done
  _describe 'groups' groups
  return 0
}


_zx "$@"
